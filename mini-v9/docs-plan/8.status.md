● Phase 1 完成 — 改动总结

  1.1 Snip + microcompact every turn ✅ (已存在)

  projectMessagesForAPI() 已自动在每次 API 请求前执行 snip projection + microcompact，无需额外改动。

  1.2 Predictive autocompact ✅

  改动文件:
  - mini-v9/src/services/compact/autoCompact.ts — 新增两个导出函数：
    - estimateMaxTurnGrowth(model) — 估算单轮 token 增长（800K 模型 ≈ 5000，200K 模型 ≈ 4000，其他 ≈ 3000）
    - getEffectiveContextWindowSize(model) — 返回完整上下文窗口大小
  - mini-v9/src/query.ts — 在 projectMessagesForAPI 后插入预测检查：
    - 如果 estimatedHeadroom < estimateMaxTurnGrowth(model)，则预判下一轮可能溢出
    - 强制触发 forceCompact: true 重新投影
    - 仅当实际压缩减少 token 时才采用压缩结果

  1.3 Model fallback ✅

  改动文件: mini-v9/src/query.ts
  - QueryOptions 新增 fallbackModel?: string 字段
  - streamError 处理段新增 fallback 逻辑：
    - retryWithBackoff 重试耗尽后，检查错误是否为 server_error / rate_limit / connection_error
    - 若有 fallbackModel 且尚未 fallback，则切换模型 + continue 重试
    - 通过 classifyAPIError() 进行错误分类

  类型检查 ✅ | 测试：仅保留预存失败（与改动无关）

  ● Both tasks are complete. Here's a summary of what was done:

  2.2 LLM-based auto-compact — Integrated llmCompact into the reactive compaction pipeline in reactiveCompact.ts:
  - Added LLM-based semantic compaction as Step 1 (best quality, after session memory compaction)
  - Falls through to microcompact → deterministic → drop-oldest strategies on failure
  - Guarded by messages.length > 6 and wrapped in try/catch to handle API failures gracefully

  2.3 Cache warning on low hit rate — Added prompt cache hit rate monitoring:
  - Created src/utils/cacheWarning.ts — tracks cache_read_input_tokens vs total input tokens per query source, warns when hit rate drops below 80% threshold
  - Added QueryCacheWarning event to src/query/transitions.ts
  - Integrated into src/query.ts:
    - Accumulates cache_creation_input_tokens and cache_read_input_tokens from message_delta events
    - After each turn, calculates cache hit rate and yields a cache_warning event if below threshold
    - Warning includes hit rate trend (↑/↓ % change since last turn)

● Ran 4 stop hooks (ctrl+o to expand)
  ⎿  Stop hook error: Hook evaluator API error: There's an issue with the selected model (kimi-k2.5). It may not exist or you may not have access to it. Run /model to
   pick a different model.

✻ Crunched for 29m 41s

※ recap: You've been working on mini-v9 — completed LLM-based auto-compact integration and cache hit rate monitoring. Both tasks are done, tests pass, no new type errors.
  Ready to pick up whatever's next.

#
当前代码是"反应式压缩"(reactive compaction)，不是"阶段式渐进折叠"(staged progressive folding)。

  两者的关键区别：

  ┌────────────┬────────────────────────────────────────────────────────┬────────────────────────────────────────────────────────────────────┐
  │    方面    │                        当前实现                        │                             阶段式折叠                             │
  ├────────────┼────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────────┤
  │ 触发时机   │ 遇到 prompt_too_long 错误后 / 预估下一轮会超限时       │ 持续监控，在多个水位线提前渐进处理                                 │
  ├────────────┼────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────────┤
  │ 折叠粒度   │ 一次性压缩，策略递进（llm → micro → keepPairs → drop） │ 按阶段逐步加深折叠程度                                             │
  ├────────────┼────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────────┤
  │ 上下文保留 │ 只保留最近 N 轮，之前的全部压缩/丢弃                   │ 按距当前距离分层处理（最近 verbatim、稍远 summary、更远 abstract） │
  └────────────┴────────────────────────────────────────────────────────┴────────────────────────────────────────────────────────────────────┘

  当前已有的类阶段式能力：
  - query.ts:106-118 的预测性自动压缩(predictive autocompact) — 当 estimatedHeadroom < growthEstimate 时提前触发 compact，算是半个阶段式
  - reactiveCompact.ts 的多级策略(step 0→5) — 逐级尝试更激进的策略

  缺失的完整阶段式折叠应该包含：

  1. 多水位线监控 — 在 context 使用率达到 40%/60%/80% 时分别触发不同策略
  2. 按距离分层 — 最近的 N 轮保持原样，中间的 M 轮做 LLM summary，更早的做 keyword-level abstract
  3. 渐进加深 — 不是一次性把 0→5 跑完，而是每次只进一级，随着 conversation 增长逐步加深
  4. 后台异步折叠 — 非阻塞地在每轮结束后对旧消息进行 summary 预处理

  需要我实现这个完整的阶段式上下文折叠吗？


# ● Phase 3 error recovery implementation complete. Here's the summary of all changes:

  Files Created (4 new)

  1. src/services/api/errors.ts (~240 lines) — Comprehensive error UX
    - getAssistantMessageFromError() — 15+ branch error-to-message converter
    - isPromptTooLongMessage() / getPromptTooLongTokenGap() — PTL detection
    - isMediaSizeError() / isMediaSizeErrorMessage() — media error detection
    - User-friendly error messages for PDF, image, auth, timeout, credit, 413 errors
    - getErrorMessageIfRefusal() — content refusal handling
  2. src/utils/imageValidation.ts (~120 lines) — Image pre-API validation
    - ImageSizeError class with user-friendly messages
    - validateImagesForAPI() — checks all base64 images against 5MB API limit
    - OversizedImage interface
  3. src/query/tokenBudget.ts (~100 lines) — Token budget system
    - createBudgetTracker() / checkTokenBudget() — per-session token limits
    - Continuation nudges + diminishing returns detection (delta < 500 tokens)
  4. src/utils/tokenBudget.ts (~80 lines) — Token budget utilities
    - parseTokenBudget() — parse "+500k" shorthand
    - findTokenBudgetPositions() — position detection for highlighting
    - getBudgetContinuationMessage() — nudge message generation

  Files Modified (2 existing)

  5. src/services/retry.ts — Retry engine enhancements
    - isStaleConnectionError() — TLS/SSL/EOF detection
    - isServerOverloadError() — 529 detection
    - isOAuthRefreshableError() — OAuth-refreshable auth error detection
    - retryWithBackoff() — persistent mode (double retries for connection errors), OAuth refresh callback, server overload cooldown (10s pause after 2 consecutive 529s)
    - withRetry() — same enhancements in legacy wrapper
  6. src/query.ts — Error withholding + 4-stage recovery
    - Stage 2 (reactive compact): Enhanced PTL error handling with better recovery events
    - Stage 3 (max tokens escalation 32K→64K): New silent escalation before recovery messages
    - Stage 4 (max tokens recovery): Enhanced with hasAttemptedMaxTokensEscalation tracking
    - Post-stream PTL detection: Catches PTL errors returned as successful API responses
    - Better recovery event yielding for UI feedback

  Verification

  - bun test — 707 pass, 1 pre-existing fail (providers.test.ts env interaction)
  - bun test compact — 21/21 pass
  - tsc --noEmit — zero type errors

● Ran 4 stop hooks (ctrl+o to expand)
  ⎿  Stop hook error: Hook evaluator API error: There's an issue with the selected model (kimi-k2.5). It may not exist or you may not have access to it. Run /model to
   pick a different model.

# Git 操作增强
 全部实现完成。以下是变更总结：

  Phase A: Git 工具函数增强

  - src/utils/git.ts — 新增 9 个函数：
    - findGitRoot() — 带 LRU 缓存（60s TTL）的 git root 查找
    - getChangedFiles() — 分 staged/unstaged/untracked/all 四组的变更文件列表
    - getFileStatus() — 单文件状态查询（staged/modified/untracked/deleted/renamed）
    - stashToCleanState() — stash 工作区变更以构建干净状态
    - getGithubRepo() — 从远程 URL 提取 owner/repo
    - findRemoteBase() — 自动检测 base 分支
    - getGitState() — 综合 git 状态快照（含变更文件列表）
    - isCurrentDirectoryBareGitRepo() — 裸仓库检测（安全用途）
  - src/utils/gitDiff.ts — 全新模块，port 自完整版 src：
    - fetchGitDiff() — 带 --shortstat 快速探针的 diff 统计
    - fetchGitDiffHunks() — 按需获取详细 hunks
    - fetchSingleFileGitDiff() — 单文件 PR 风格 diff
    - parseGitNumstat() / parseGitDiff() / parseShortstat() — 解析器
  - src/utils/gitSettings.ts — 极简 git 用户配置读取
  - src/context.ts — 增强 git 上下文注入：
    - 新增 git user name 显示
    - 新增最近 5 条 commit 记录
    - enhanced context 中新增 recent commits

  Phase B: 专用 Git 工具

  - GitDiffTool — 显示工作区 diff 统计和 hunks，支持单文件 diff
  - GitLogTool — 可配置的 git log（maxCount/path/author/format/branch 过滤）
  - GitStatusTool — 综合仓库状态（分支/commit/cleanliness/ahead-behind/变更文件列表）
  - 3 个工具已注册到 tools.ts → getAllBaseTools()

  Phase C: Worktree 隔离 + 安全

  - src/utils/worktree.ts — worktree 创建/删除/列出
  - src/agents/forkSubagent.ts — 实际执行 worktree 创建（isolation='worktree' 时创建临时 worktree，完成后自动清理）
  - src/agents/agentRunner.ts — 添加 cwdOverride 支持
  - BashTool.ts — 添加裸仓库检测，阻止沙箱逃逸

  测试

  - 24 个 git 测试全部通过（原有 13 + 新增 11）
  - 12 个 toolsRegistry 测试全部通过（工具计数 43→46，新增 git 工具名称检查）
  - 全量测试：720 pass, 2 fail（1 个 pre-existing provider 测试 + 1 个已修复的 tools 计数）