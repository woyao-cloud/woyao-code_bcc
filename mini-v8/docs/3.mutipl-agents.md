计划概要

  5 个 Phase，按依赖关系推进：

  ┌───────────────────┬───────────────────────────────────────────────────────────────────┬──────────┬──────────┐
  │       Phase       │                               目标                                │ 新增文件 │ 修改文件 │
  ├───────────────────┼───────────────────────────────────────────────────────────────────┼──────────┼──────────┤
  │ 1. Async 执行     │ AgentTool 不阻塞父 agent，支持 run_in_background，多个 agent 并发 │ 0        │ 4        │
  ├───────────────────┼───────────────────────────────────────────────────────────────────┼──────────┼──────────┤
  │ 2. ALS 上下文隔离 │ AsyncLocalStorage 替代 Map 栈，嵌套 agent 互不干扰                │ 1        │ 3        │
  ├───────────────────┼───────────────────────────────────────────────────────────────────┼──────────┼──────────┤
  │ 3. Agent Memory   │ MEMORY.md + topic 文件跨会话持久化，snapshot 同步                 │ 2        │ 3        │
  ├───────────────────┼───────────────────────────────────────────────────────────────────┼──────────┼──────────┤
  │ 4. Mailbox 通信   │ 文件 inbox + lockfile + structured protocol 消息路由              │ 2        │ 5        │
  ├───────────────────┼───────────────────────────────────────────────────────────────────┼──────────┼──────────┤
  │ 5. 通知系统       │ task-notification XML + commandQueue 优先级投递                   │ 1        │ 2        │
  └───────────────────┴───────────────────────────────────────────────────────────────────┴──────────┴──────────┘

  依赖关系

  Phase 1 (Async) ──┬── Phase 2 (ALS)
                    ├── Phase 3 (Memory)        ← 可与 Phase 2 并行
                    ├── Phase 4 (Mailbox)       ← 依赖 Phase 2
                    └── Phase 5 (Notifications)

  核心设计决策

  - 不引入 React/Ink — mailboxPoller 用纯 async 轮询替代 useInboxPoller hook，保持 mini-v8 无 UI 架构
  - ALS 而非 Map — 用 Node.js AsyncLocalStorage 解决并发 agent 上下文冲突（Bun 完整支持）
  - 通知队列用模块级数组 — 非 React state，CLI 流循环可直接 drain，无需引入 Zustand
  - 保留同步 API — runAgentSync 保持现有行为不变，runAgentAsync 是新增能力

  预计新增约 1,200-1,500 行代码 + 5 个测试文件。

  # phase 1
   Phase 1 完成

  424 测试通过，1 个是环境变量冲突的预存失败，零回归。

  改动总结

  ┌──────────────────────────────────────┬──────┬────────────────────────────────────────────────────────────────────────────────────────────┐
  │                 文件                 │ 动作 │                                            内容                                            │
  ├──────────────────────────────────────┼──────┼────────────────────────────────────────────────────────────────────────────────────────────┤
  │ agents/agentTypes.ts                 │ 修改 │ 新增 AgentProgress, AgentTaskState, AgentTaskStatus, TaskId                                │
  ├──────────────────────────────────────┼──────┼────────────────────────────────────────────────────────────────────────────────────────────┤
  │ agents/agentRunner.ts                │ 重写 │ runAgentSync (原逻辑) + runAgentAsync (fire-and-forget) + runAgentLoopCore (共享核心)      │
  ├──────────────────────────────────────┼──────┼────────────────────────────────────────────────────────────────────────────────────────────┤
  │ services/taskStore.ts                │ 重写 │ 保留原 Task CRUD + 新增 AgentTaskStore 单例（create/get/list/complete/fail/kill/progress） │
  ├──────────────────────────────────────┼──────┼────────────────────────────────────────────────────────────────────────────────────────────┤
  │ tools/builtin/AgentTool/AgentTool.ts │ 重写 │ schema 增加 run_in_background，execute 分叉 sync/async 路径                                │
  ├──────────────────────────────────────┼──────┼────────────────────────────────────────────────────────────────────────────────────────────┤
  │ __tests__/agentRunner.async.test.ts  │ 新增 │ 19 个测试覆盖 AgentTaskStore 全部生命周期和 runAgentAsync 结构                             │
  └──────────────────────────────────────┴──────┴────────────────────────────────────────────────────────────────────────────────────────────┘

  核心能力

  - runAgentSync() — 阻塞等待，行为与改前完全一致
  - runAgentAsync() — 立即返回 AgentTaskState，后台执行，通过 agentTaskStore 追踪进度
  - AbortController 隔离 — 异步 agent 拥有独立的 AbortController，父级取消不影响后台任务
  - agentTaskStore — 完整的生命周期管理：running → completed/failed/killed，支持进度更新和批量 kill

  # phase 3

  Phase 3 已完成。以下是改动总结：
修复 1: Memory prompt 死代码（关键 bug）
agentRunner.ts:34 中 loadAgentMemoryPrompt() 一直被 import 但从未调用，导致 Explore/Plan 等带 memory scope 的 agent 实际运行时完全没有 memory prompt。
- 新增 buildAgentSystemPrompt() 函数（第 82-104 行），在构建 agent system prompt 时自动：
  1. 调用 ensureAgentMemoryDir() 确保 memory 目录存在
  2. 调用 loadAgentMemoryPrompt() 读取持久化记忆内容
  3. 拼接到 system prompt 末尾
- runAgentSync 和 runAgentAsync 两处 getSystemPrompt() 调用改为 buildAgentSystemPrompt()
修复 2: 内存 agent 自动注入文件工具
filterToolsForAgent() 新增逻辑：当 agent 声明了 memory 且使用特定工具列表（非 ['*']）时，自动确保 Write、Edit、Read 三个工具可用，让 agent 能读写自己的 memory 目录。
附带修复
agentRunner.async.test.ts — 移除了从 Phase 2 重构后遗留的死 import（getCurrentAgentContext、getAgentContext 已迁移至 utils/agentContext.ts）。

# phase 4
Phase 4 已完成。改动总结：
新增文件
src/agents/teammateMailbox.ts (215行)
- TeammateMessage 接口 — 标准消息格式
- 邮箱 CRUD：writeToMailbox / readMailbox / readUnreadMessages / markMessagesAsRead / compactMailbox
- 目录管理：initializeTeamMailboxes / cleanupTeamMailboxes / removeInboxFile
- 协议消息支持：sendProtocolMessage / isProtocolMessage / parseProtocolMessage
- 结构化通知：sendIdleNotification / sendShutdownRequest / sendShutdownResponse
- 自动压缩：消息超过100条时保留未读 + 最近50条已读
- setMailboxBaseDir() 测试支持
src/agents/mailboxPoller.ts (41行)
- waitForNextMessage() — Worker 侧轮询，支持 AbortSignal 取消、shutdown_request 协议短路
- PollOptions 可配置：轮询间隔 (默认500ms)、待处理消息去重
修改文件
src/agents/teamManager.ts
- createTeam → 自动创建 inbox 目录、给 lead 发 team_created 欢迎消息
- deleteTeam → 清理 inbox 文件
- addTeamMember → 为新成员创建空 inbox
- removeTeamMember → 删除成员对应 inbox 文件
- setTeamsBaseDir → 同步同步 mailbox 基准目录
循环状态
| Phase | 状态 |
|-------|------|
| Phase 1 (Async) | ✅ 已完成 |
| Phase 2 (ALS) | ✅ 已完成 |
| Phase 3 (Memory) | ✅ 已完成 (含关键 bug 修复) |
| Phase 4 (Mailbox) | ✅ 已完成 |
| Phase 5 (Notifications) | ❌ 未开始 |