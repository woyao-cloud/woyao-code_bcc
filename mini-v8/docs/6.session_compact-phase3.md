● Phase 3 — Token Budget & Reactive Integration

  现状问题

  经过 Phase 1 & 2 后还剩下 4 个关键 gap：

  Issue 1: Reactive compact 在 query.ts 中未正确集成
  query.ts:201 调用 reactiveCompact(conversation.fullMessages) 后把结果写回本地 messages 数组而非 conversation.fullMessages，导致下次 projectMessagesForAPI
  仍使用旧数据。也没有调 runPostCompactCleanup()。

  Issue 2: Cached microcompact 未接入投影管线
  projectMessagesForAPI() 第 257 行直接调 microcompactToolResults()，每次都全量扫描。Phase 2 已经实现了 cachedMicrocompact.ts，但还没接进管线。

  Issue 3: buildCompactBoundaryMetadata 摘要检测字符串过时
  第 393 行检查 'Earlier conversation'，但 Phase 1 已将摘要格式改为 '[Compacted'。导致 compact boundary 元数据中的 summaryPreview 始终为空。

  Issue 4: 投影管线缺少 token 压力报告
  APIMessageProjection 没有返回剩余 headroom，调用方不知道离上下文窗口多近。

  ---
  优化方案

  ┌─────┬────────────────────────────────────────────┬──────────────────┬────────┐
  │  #  │                    优化                    │     涉及文件     │ 改动量 │
  ├─────┼────────────────────────────────────────────┼──────────────────┼────────┤
  │ 3-1 │ 修复 reactive compact 在 query.ts 中的集成 │ query.ts         │ ~15 行 │
  ├─────┼────────────────────────────────────────────┼──────────────────┼────────┤
  │ 3-2 │ 接入 cached microcompact 到投影管线        │ apiProjection.ts │ ~10 行 │
  ├─────┼────────────────────────────────────────────┼──────────────────┼────────┤
  │ 3-3 │ 修复 boundary metadata 摘要检测字符串      │ apiProjection.ts │ ~2 行  │
  ├─────┼────────────────────────────────────────────┼──────────────────┼────────┤
  │ 3-4 │ 投影结果添加 token 压力报告                │ apiProjection.ts │ ~15 行 │
  └─────┴────────────────────────────────────────────┴──────────────────┴────────┘

  3-1: 修复 reactive compact 集成
  - 将 reactiveCompact() 的结果通过 commitCompactedProjectionToConversation 写回 conversation.fullMessages
  - 调用 runPostCompactCleanup()
  - 使用 pushMessageWithUuid() 保持 UUID 对齐

  3-2: 接入 cached microcompact
  - 在 projectMessagesForAPI 中创建/持有 CachedMCState 实例
  - 替换 microcompactToolResults() 调用为 getMicrocompactWithCache(state, messages)

  3-3: 修复 summary 检测字符串
  - message.content.includes('Earlier conversation')
  + message.content.includes('Compacted')

  3-4: Token 压力报告
  在 APIMessageProjection 中新增：
  estimatedHeadroom: number  // context window - estimated tokens
  headroomRatio: number       // 0.0 ~ 1.0, 越低越危险

  ---
  总改动约 5 个文件、~40 行，不影响现有测试。可以开始吗？


   Phase 3 complete. All changes verified (624 pass, 1 pre-existing env issue).

  ┌─────┬────────────────────────────┬──────┬───────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │  #  │            优化            │ 状态 │                                                 文件  
  ├─────┼────────────────────────────┼──────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 3-1 │ 修复 reactive compact 集成 │ ✅   │ query.ts — 结果写回 conversation.fullMessages、修剪 UUID、重置 budget state、调用 runPostCompactCleanup() │
  ├─────┼────────────────────────────┼──────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 3-2 │ 接入 cached microcompact   │ ✅   │ apiProjection.ts — 模块级 CachedMCState 实例，getMicrocompactWithCache() 替代 microcompactToolResults()   │
  ├─────┼────────────────────────────┼──────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 3-3 │ 修复 summary 检测字符串    │ ✅   │ apiProjection.ts:399 — 'Earlier conversation' → 'Compacted'
  ├─────┼────────────────────────────┼──────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 3-4 │ Token 压力报告             │ ✅   │ APIMessageProjection 新增 estimatedHeadroom、headroomRatio 