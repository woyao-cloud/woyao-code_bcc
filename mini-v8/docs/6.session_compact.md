 mini-v8 会话历史压缩优化计划

  现状分析

  mini-v8 已有基础的 token 优化管线：microcompact → tool result budget → full compaction。但相比主代码库，有 7 个关键优化缺失。

  ---
  Phase 1: 低投入、高回报（立即减少 token）

  ┌─────┬──────────────────────┬──────────────────────────────────────────────────────────────────────────────────┬────────────────────────────────────────────────┬──────┐
  │  #  │         优化         │                                       原理                                       │                    预期节省                    │ 难度 │
  ├─────┼──────────────────────┼──────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────┼──────┤
  │ 1   │ 动态紧缩阈值         │ 当前固定 70% 窗口。改用主库的按模型分档：800K+ 模型 50K 余量、400K+ 30K、200K+   │ 提前触发缩减，避免突增到 90%+                  │ 小   │
  │     │                      │ 20K                                                                              │                                                │      │
  ├─────┼──────────────────────┼──────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────┼──────┤
  │ 2   │ 改进紧缩摘要质量     │ 当前只截取前 5 条用户消息的前 100 字符。改用 sessionMemory                       │ 压缩后的摘要保留更多有用信息，减少后续追问     │ 小   │
  │     │                      │ 存储有意义的上下文，紧缩时生成结构化摘要                                         │ token                                          │      │
  ├─────┼──────────────────────┼──────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────┼──────┤
  │ 3   │ Tool result          │ 超大 tool result（如 Read 整个文件）持久化到磁盘，替换为 <persisted-output       │ 单次可节省数千到数万 token                     │ 中   │
  │     │ 落盘持久化           │ id="..."> 引用                                                                   │                                                │      │
  └─────┴──────────────────────┴──────────────────────────────────────────────────────────────────────────────────┴────────────────────────────────────────────────┴──────┘

  Phase 2: 新功能引入（中等投入）

  ┌─────┬────────────────────────┬────────────────────────────────────────────────────────────────────────────────┬────────────────────────────────────────────────┬──────┐
  │  #  │          优化          │                                      原理                                      │                    预期节省                    │ 难度 │
  ├─────┼────────────────────────┼────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────┼──────┤
  │ 4   │ Snip                   │ 注入 snip_boundary 系统消息，标记要移除的 UUID 列表，下次 API 调用前物理移除   │ 从消息数组中彻底删除不再需要的旧消息           │ 中   │
  │     │ Compact（历史裁剪）    │                                                                                │                                                │      │
  ├─────┼────────────────────────┼────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────┼──────┤
  │ 5   │ Cached Microcompact    │ 利用 Anthropic 4.x 的 cache_edits API，在服务端删除旧 tool result              │ 避免 cache miss 的全量重发成本（通常           │ 高   │
  │     │                        │ 而不本地修改消息，保留 prompt cache                                            │ $0.10-0.50/次）                                │      │
  ├─────┼────────────────────────┼────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────┼──────┤
  │ 6   │ Post-Compact Cleanup   │ 紧缩后清理：重置 microcompact 状态、清空系统上下文缓存、清空消息缓存           │ 避免状态不一致导致的额外 token 发送            │ 中   │
  └─────┴────────────────────────┴────────────────────────────────────────────────────────────────────────────────┴────────────────────────────────────────────────┴──────┘

  Phase 3: 高级（较高投入）

  ┌─────┬─────────────────────────┬──────────────────────────────────────────────────────────┬──────────────────────────────┬──────┐
  │  #  │          优化           │                           原理                           │           预期节省           │ 难度 │
  ├─────┼─────────────────────────┼──────────────────────────────────────────────────────────┼──────────────────────────────┼──────┤
  │ 7   │ Time-based microcompact │ 根据消息距今时间自动清除旧 tool result，而非仅靠数量阈值 │ 长时间对话中自然淘汰过期结果 │ 中   │
  └─────┴─────────────────────────┴──────────────────────────────────────────────────────────┴──────────────────────────────┴──────┘

  ---
  具体实施建议

  Phase 1-1: 动态紧缩阈值

  mini-v8/src/services/compact/autoCompact.ts 中的 needsCompaction() 当前用 TOKEN_LIMIT_RATIO = 0.7。改造为：

  function getCompactBuffer(model?: string): number {
    const maxTokens = getMaxTokens(model ?? '')
    if (maxTokens >= 800_000) return 50_000
    if (maxTokens >= 400_000) return 30_000
    if (maxTokens >= 200_000) return 20_000
    return 10_000
  }

  export function needsCompaction(messages, model?: string): boolean {
    return estimateTokens(messages) > getMaxTokens(model) - getCompactBuffer(model)
  }

  Phase 1-2: 改进紧缩摘要

  当前 generateCompactionSummary() 只截取原文。改用 sessionMemory.ts 中的 getSessionMemorySummaryForCompact() →
  在对话过程中积累结构化摘要（用户请求、决策、文件路径），紧缩时生成有意义的总结。

  Phase 1-3: Tool result 落盘

  mini-v8/src/query.ts 已 import persistLargeToolResult（line 26），但未使用。激活此函数，配合 main 库的 toolResultStorage.ts 逻辑，将超大 tool result 替换为磁盘引用。

  Phase 2-4: Snip Compact

  从主库移植 snipCompact.ts 的核心逻辑：
  - 在消息数组中注入 snip_boundary 系统消息
  - 在 projectMessagesForAPI() 中调用 snipCompactIfNeeded()
  - 物理移除已标记的旧消息

  Phase 2-5: Cached Microcompact

  移植 cachedMicrocompact.ts 的 registerToolResult() / createCacheEditsBlock() 逻辑。需要在 API client（services/api/claude.ts）中支持发送 cache_edits 和 cache_reference 块。

  Phase 2-6: Post-Compact Cleanup

  移植 postCompactCleanup.ts 的核心清理调用链，挂接到 compactMessages() 完成后执行。

  ---
  路线图建议

  Phase 1 (本周)
  ├── 1-1 动态紧缩阈值     — 3 个函数改动，无新依赖
  ├── 1-2 改进紧缩摘要     — 增强 sessionMemory，改动 compact 逻辑
  └── 1-3 Tool result 落盘  — 激活已有 persistLargeToolResult

  Phase 2 (下周)
  ├── 2-4 Snip Compact      — 移植 ~200 行核心逻辑
  ├── 2-5 Cached Microcompact — 需要 API 层支持 cache_edits
  └── 2-6 Post-Compact Cleanup — 移植 ~150 行

  Phase 3 (后续)
  └── 3-7 Time-based microcompact — 按时间阈值自动清除

  Phase 1 预估可节省 30-50% 的每次 API 调用 token 数（对话越长效果越明显）。Phase 2 在此基础上再节省 10-20% 且改善响应速度（通过 cache 命中）。

  需要我展开某个具体优化的实现细节吗？

✻ Brewed for 10m 12s

> 展开Phase 1 优化细节

● Let me read the relevant files in detail to provide precise implementation guidance.

  Read 5 files (ctrl+o to expand)

● 好的，以下是 Phase 1 三个优化的完整实现细节，包含具体代码改动。

  ---
  Phase 1 实现细节

  1-1 动态紧缩阈值

  目标文件: mini-v8/src/services/compact/autoCompact.ts

  当前问题: needsCompaction() 使用固定比例 TOKEN_LIMIT_RATIO = 0.7，对 200K 模型用 140K 阈值合理，但对 32K 模型用 22.4K
  阈值就太宽松（接近填满）。且没有考虑模型的实际上下文窗口。

  改动:

  // 删除旧的：
  const TOKEN_LIMIT_RATIO = 0.7
  const ESTIMATED_MAX_TOKENS = 100_000

  // 新增（放在文件顶部常量区）：
  const COMPACT_BUFFER_LARGE = 50_000    // 800K+ 模型，保留 50K 余量
  const COMPACT_BUFFER_MEDIUM = 30_000   // 400K+ 模型，保留 30K 余量
  const COMPACT_BUFFER_SMALL = 20_000    // 200K+ 模型，保留 20K 余量
  const COMPACT_BUFFER_TINY = 10_000     // 其他模型，保留 10K 余量
  const ESTIMATED_MAX_TOKENS = 200_000   // 调高兜底值，适配 1M 模型时代

  新增函数（插在 getEstimatedContextWindow 之前或之后）：

  function getCompactBuffer(model?: string): number {
    if (!model) return COMPACT_BUFFER_TINY
    const maxTokens = getEstimatedContextWindow(model)
    if (maxTokens >= 800_000) return COMPACT_BUFFER_LARGE
    if (maxTokens >= 400_000) return COMPACT_BUFFER_MEDIUM
    if (maxTokens >= 200_000) return COMPACT_BUFFER_SMALL
    return COMPACT_BUFFER_TINY
  }

  修改 needsCompaction()：

  // 旧代码：
  export function needsCompaction(
    messages: BetaMessageParam[],
    model?: string,
  ): boolean {
    const tokens = estimateTokens(messages)
    return tokens > getEstimatedContextWindow(model) * TOKEN_LIMIT_RATIO
  }

  // 新代码：
  export function needsCompaction(
    messages: BetaMessageParam[],
    model?: string,
  ): boolean {
    const tokens = estimateTokens(messages)
    const window = getEstimatedContextWindow(model)
    const buffer = getCompactBuffer(model)
    // 当已用 token > 窗口 - 余量 时触发紧缩
    return tokens > window - buffer
  }

  效果对比:

  ┌────────────────────────┬───────────────┬───────────────────────┬──────────────────────────────┐
  │          模型          │ 旧阈值（70%） │ 新阈值（窗口 - 余量） │             差异             │
  ├────────────────────────┼───────────────┼───────────────────────┼──────────────────────────────┤
  │ claude-sonnet-4 (128K) │ 89.6K         │ 98K (128K-30K)        │ 更晚触发，给模型留更多上下文 │
  ├────────────────────────┼───────────────┼───────────────────────┼──────────────────────────────┤
  │ claude-opus-4 (200K)   │ 140K          │ 180K (200K-20K)       │ 更晚触发                     │
  ├────────────────────────┼───────────────┼───────────────────────┼──────────────────────────────┤
  │ qwen-* (32K)           │ 22.4K         │ 22K (32K-10K)         │ 几乎不变                     │
  ├────────────────────────┼───────────────┼───────────────────────┼──────────────────────────────┤
  │ deepseek (128K)        │ 89.6K         │ 98K (128K-30K)        │ 更晚触发                     │
  └────────────────────────┴───────────────┴───────────────────────┴──────────────────────────────┘

  原理: 大窗口模型（如 Opus 4 的 200K）不需要提前到 70% 就紧缩，保留更多有效上下文；小窗口模型保持积极的紧缩策略。同时，由于 compactMessages 保留最近 3
  对消息（默认），新阈值能保证紧缩后的消息仍有充足空间。

  ---
  1-2 改进紧缩摘要质量

  目标文件: mini-v8/src/services/compact/autoCompact.ts + mini-v8/src/services/memory/sessionMemory.ts

  当前问题:
  1. sessionMemory 默认 enabled: false（第 36 行），根本没用
  2. generateCompactionSummary() 只截取前 5 条用户消息的前 100 字符，基本没有语义信息
  3. compactMessages() 的 buildSessionMemoryCompactionSummary() 虽然调用了 session memory，但因为 enabled: false，实际 getSessionMemorySummaryForCompact() 返回空字符串

  改动 A — 启用 session memory（sessionMemory.ts）:

  // 第 36 行，改默认配置：
  const DEFAULT_CONFIG: SessionMemoryConfig = {
    enabled: true,          // false → true
    minTokensForInit: 2000,
    minTokensBetweenUpdate: 1000,
    maxNotes: 30,
  }

  改动 B — 改进 generateCompactionSummary()（autoCompact.ts）:

  // 替换旧函数：
  export function generateCompactionSummary(removed: BetaMessageParam[]): string {
    if (removed.length === 0) return ''

    const userMessages = removed.filter(m => m.role === 'user')
    const assistantMessages = removed.filter(m => m.role === 'assistant')

    // 统计工具调用
    const toolCalls = new Set<string>()
    for (const msg of removed) {
      if (msg.role !== 'assistant' || !Array.isArray(msg.content)) continue
      for (const block of msg.content) {
        if (isToolUseBlock(block)) toolCalls.add(block.name)
      }
    }

    // 提取用户意图：取每条用户消息的前 200 字符
    const intents = userMessages.slice(-4).map(m => {
      const content = typeof m.content === 'string' ? m.content : ''
      return content.slice(0, 200).trim()
    }).filter(Boolean)

    // 提取关键决策：取助手回复中包含决策关键字的部分
    const decisions = assistantMessages.slice(-3).map(m => {
      const text = typeof m.content === 'string' ? m.content : ''
      const match = text.match(/(?:I'?ll|Let's|We should|The plan is)[^.]*\./gi)
      return match ? match.slice(0, 2).join('; ') : ''
    }).filter(Boolean)

    const parts: string[] = [
      `[Compacted ${removed.length} messages:`,
      `${userMessages.length} user requests`,
      `${assistantMessages.length} assistant responses`,
    ]

    if (toolCalls.size > 0) {
      parts.push(`tools used: ${[...toolCalls].join(', ')}`)
    }

    if (intents.length > 0) {
      parts.push(`requests: ${intents.join(' | ')}`)
    }

    if (decisions.length > 0) {
      parts.push(`decisions: ${decisions.join(' | ')}`)
    }

    parts.push(']')
    return parts.join('\n')
  }

  效果对比:

  ┌──────────┬─────────────────────────────────────────────────────────────────────────────┬───────────────────────────────────────────────────────────────────┐
  │   维度   │                                   旧摘要                                    │                              新摘要                               │
  ├──────────┼─────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ 格式     │ [Earlier conversation summary: 12 messages covering: - fix bug; - add test] │ 结构化多行，含工具调用统计和决策                                  │
  ├──────────┼─────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ 用户意图 │ 前 100 字符，可能截断在单词中间                                             │ 前 200 字符，精确提取                                             │
  ├──────────┼─────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ 工具调用 │ 无                                                                          │ tools used: Bash, Read, Grep                                      │
  ├──────────┼─────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ 决策提取 │ 无                                                                          │ decisions: I'll fix the error handler; Let's add input validation │
  ├──────────┼─────────────────────────────────────────────────────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ 助手回复 │ 忽略                                                                        │ 最近 3 条提取决策句                                               │
  └──────────┴─────────────────────────────────────────────────────────────────────────────┴───────────────────────────────────────────────────────────────────┘

  改动 C — 确保紧缩时传递 session memory 摘要（autoCompact.ts）:

  compactMessages() 已有 buildSessionMemoryCompactionSummary() 逻辑，只需确保：
  1. getSessionMemorySummaryForCompact() 在 sessionMemory.ts 中默认被调用（已启用 enabled: true 后即可）
  2. apiProjection.ts 中的 projectMessagesForAPI() 已调用 getSessionMemorySummaryForCompact()

  无需额外改动，上述改动 B 作为 fallback 在 session memory 为空时使用。

  ---
  1-3 Tool Result 落盘持久化

  目标文件: mini-v8/src/query.ts

  当前问题: query.ts 第 26 行已 import persistLargeToolResult，但从未调用。tool result 直接 push 到 messages，大结果（文件内容、shell 输出）全部在消息体中传递。

  背景: toolResultStorage.ts 已经完整实现：
  - persistLargeToolResult(value) — 当字符串长度 > 100K 时写入临时文件，返回 <persisted-output>/path/to/file</persisted-output> 替换文本
  - restorePersistedToolResult(value) — 从 <persisted-output> 标签恢复原始内容
  - cleanupAllPersistedResults() — 进程退出时清理

  改动 — 在 query.ts 中，tool result push 到 messages 之前用 persistLargeToolResult 处理：

  // 在 query.ts 第 359 行附近，修改：
  // 旧代码：
  messages.push({ role: 'user', content: toolResults })

  // 新代码：
  const persistedResults = toolResults.map(tr => {
    const raw = tr as unknown as Record<string, unknown>
    if (raw.content && typeof raw.content === 'string') {
      const persisted = persistLargeToolResult(raw.content)
      if (persisted !== raw.content) {
        return { ...tr, content: persisted } as typeof tr
      }
    }
    return tr
  })
  messages.push({ role: 'user', content: persistedResults })

  同时修改 tool result 的 yield，使下游能拿到原始内容（用于显示）但消息体存引用：

  // 第 346-357 行附近，修改：
  for (const tr of toolResults) {
    const raw = tr as unknown as Record<string, unknown>
    const rawContent = typeof raw.content === 'string' ? raw.content : ''
    // 对 yield 直接透传原始内容（UI 展示不受影响）
    const content = rawContent
    yield {
      type: 'tool_result' as const,
      id: raw.tool_use_id as string,
      name: toolUses.find(tu => tu.id === raw.tool_use_id)?.name ?? 'unknown',
      success: !raw.is_error,
      content,
      isError: raw.is_error === true,
    }
    // persist 只作用于 messages 数组中的存储
  }

  关键决策: persist 只压缩 messages 数组中的消息体，不影响 yield 到 UI 的原始内容。这样用户看到的输出完整，但下一次 API 调用时只发送引用。

  效果: 对于 Read 一个 500K 文件的场景，tool result 从 500K 字符 → ~70 字符的 <persisted-output> 引用，单次节省约 125K tokens。

  ---
  改动汇总

  ┌──────────────────┬─────────────────────────────────────────────────────────┬──────────────────┐
  │       文件       │                        改动类型                         │     行数变化     │
  ├──────────────────┼─────────────────────────────────────────────────────────┼──────────────────┤
  │ autoCompact.ts   │ 修改 3 处常量 + 新增 1 函数 + 修改 1 函数 + 替换 1 函数 │ +25 / -5         │
  ├──────────────────┼─────────────────────────────────────────────────────────┼──────────────────┤
  │ sessionMemory.ts │ 改 1 个默认值                                           │ +0 / -0 (1 字符) │
  ├──────────────────┼─────────────────────────────────────────────────────────┼──────────────────┤
  │ query.ts         │ 修改 1 处（tool result push 前加 persist）              │ +10 / -1         │
  └──────────────────┴─────────────────────────────────────────────────────────┴──────────────────┘

  总计新增约 35 行，删除 6 行。

  三个改动相互独立，可以并行实施。要我直接开始写入代码吗？

✻ Brewed for 1m 10s

> 开始

✻ 1-1 & 1-2: Dynamic thresholds + better compaction summaries… (1m 0s · ↓ 490 tokens · thought for 3s)
  ⎿  ■ 1-1 & 1-2: Dynamic thresholds + better compaction summaries
     □ 1-2: Enable session memory by default
     □ 1-3: Tool result disk persistence in query.ts
     □ Run tests to verify

────────────────────────────────────────────────────────────────────────────────────────────────────
 Phase 1 全部完成。624 tests pass, 0 个因我们的改动而失败（唯一的 1 个 model.test.ts 失败是环境变量预先配置的 deepseek 所致，与本次改动无关）。

  ---
  改动总结

  ┌──────┬───────────────────┬──────────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │  #   │       优化        │       文件       │                                                          改动                                                           │

● PowerShell(echo "ANTHROPIC_MODEL=$env:ANTHROPIC_MODEL"; echo "ANTHROPIC_DEFAULT_SONNET_MODEL=$env:ANTHROPIC_DEFAULT_SONNET_MODEL"; echo
            "ANTHROPIC_DEFAULT_OPUS_MODEL=$env:…)
  ⎿  ANTHROPIC_MODEL=deepseek-v4-flash:cloud
     ANTHROPIC_DEFAULT_SONNET_MODEL=deepseek-v4-flash:cloud
     ANTHROPIC_DEFAULT_OPUS_MODEL=deepseek-v4-flash:cloud
     ANTHROPIC_DEFAULT_HAIKU_MODEL=deepseek-v4-flash:cloud

● Confirmed — this is a pre-existing environment configuration issue, completely unrelated to our changes.

● Phase 1 全部完成。624 tests pass, 0 个因我们的改动而失败（唯一的 1 个 model.test.ts 失败是环境变量预先配置的 deepseek 所致，与本次改动无关）。

  ---
  改动总结

  ┌──────┬───────────────────┬──────────────────┬─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │  #   │       优化        │       文件       │                                                          改动                                                           │
  ├──────┼───────────────────┼──────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 1-1  │ 动态紧缩阈值      │ autoCompact.ts   │ 新增 getCompactBuffer()，按模型窗口分档（800K+→50K 余量、400K+→30K、200K+→20K、小模型→30%）。needsCompaction() 改为     │
  │      │                   │                  │ tokens > window - buffer                                                                                                │
  ├──────┼───────────────────┼──────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 1-2a │ 改进紧缩摘要      │ autoCompact.ts   │ generateCompactionSummary() 重写：提取工具调用统计、用户意图（后 4 条各 200 字符）、助手决策句（后 3                    │
  │      │                   │                  │ 条），输出结构化多行摘要                                                                                                │
  ├──────┼───────────────────┼──────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 1-2b │ 启用 Session      │ sessionMemory.ts │ DEFAULT_CONFIG.enabled 从 false → true，紧缩时自动使用会话内存生成有意义摘要                                            │
  │      │ Memory            │                  │                                                                                                                         │
  ├──────┼───────────────────┼──────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 1-3a │ Tool result 落盘  │ query.ts         │ tool result push 前调用 persistLargeToolResult()，大结果（>100K 字符）写入临时文件，消息体保留引用                      │
  ├──────┼───────────────────┼──────────────────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ 1-3b │ 落盘恢复          │ apiProjection.ts │ projectMessagesForAPI() 起始处新增 restore 步骤，调用 restorePersistedToolResult() 还原内容后再执行 microcompact/budget │
  └──────┴───────────────────┴──────────────────┴─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
