• 现在再看一遍 src/ 和当前 mini-v8，结论已经比较明确了：

  mini-v8 在“降 token 主链”上其实已经补了不少，不再是一个只会无脑塞全量历史的版本。它现在已经有：

  - fullMessages / messagesForAPI 双层结构，mini-v8/src/services/messages/apiProjection.ts:29
  - 每轮按预算投影、先 microcompact 再 full compact，mini-v8/src/services/messages/apiProjection.ts:59
  - full compact 优先消费 session memory summary，mini-v8/src/services/messages/apiProjection.ts:76
  - system context 的按需注入、预算选择器、query-aware memory/agent 检索，mini-v8/src/context.ts:243 mini-v8/src/
    context.ts:337 mini-v8/src/context.ts:398

  和 src 比，当前真正还没补齐、而且继续做最值的，不是“再发明一种 compact”，而是下面这几项。

  差异判断

  src 更成熟的地方主要有 4 类：

  1. system context 的缓存和失效机制更完整
     src/context.ts:116 是 conversation 级 memoize，不只是局部缓存 CLAUDE.md / skills。
     另外 systemPromptSections 明确支持 section cache 与 /clear、/compact 失效，src/constants/
     systemPromptSections.ts:17。
  2. tool result 的预算控制细很多
     src 不是粗粒度“老 tool result 统一 stub”，而是按 message 分组、按 tool_use_id 冻结决策、持久化 preview、resume 后重
     建 replacement state，核心在 src/utils/toolResultStorage.ts:739 和 src/utils/toolResultStorage.ts:939。
     这比 mini-v8 当前的 microcompact 更稳，也更省 token。
  3. compact boundary / resume 语义更完整
     src 会把 compact boundary 当成一等消息处理，并在 compact 后释放 pre-boundary history，同时兼顾 transcript/resume，
     src/QueryEngine.ts:700 src/QueryEngine.ts:957。
     mini-v8 现在更偏“projection 优先”，对长期 resume/debug 语义还没完全补齐。
  4. session memory / autocompact 整体链路更老练
     src 这块有更多防抖、阈值、fork/cache-safe 处理，src/services/compact/autoCompact.ts:316 和 src/services/
     SessionMemory/sessionMemory.ts:323。
     不过 mini-v8 已经拿到了最核心的 token-saving 思路，差距主要在工程稳健性，不在方向。

  给 mini-v8 的下一阶段优化路线

  P6 我建议先做 system context 全量 memoization + 明确失效。

  - 现在 mini-v8 只缓存了 CLAUDE.md 和 skills 片段，mini-v8/src/context.ts:111
  - 还缺 conversation 级 getSystemContext() 缓存
  - 同时要在 /clear、/compact、cwd 变化、memory/agent/teams 变化时失效
  - 这是低风险、稳定省 token 和省 latency 的项

  P7 做 query-aware teams + team memory 真正接线。

  - 现在 teams 还是 getTeamsForPrompt() 直接拿，mini-v8/src/context.ts:302
  - includeTeamMemory 目前基本是空位，mini-v8/src/context.ts:320
  - 这块补齐后，agent/team 类 prompt 注入会更收敛，不会长期把无关团队信息带进系统提示

  P8 做更细粒度的 tool result budgeting。

  - 目标不是替换现有 microcompact，而是在 messagesForAPI 投影阶段叠加一层“按 message / per-result budget”的预算器
  - 学 src/utils/toolResultStorage.ts 的三点：
      - replacement 决策按 tool_use_id 冻结
      - preview/stub 可复用，避免每轮重复变化影响 cache
      - resume 后可重建 replacement state
  - 这是继续降 token 的最高价值项之一，尤其是工具输出大的场景

  P9 做 compact boundary 元数据和 resume-aware projection。

  - 现在 mini-v8 的 fullMessages / messagesForAPI 已经把“展示”和“发 API”分开了，这是对的
  - 下一步是给 compact 结果打边界，让历史折叠后的语义可恢复、可调试、可 resume
  - 这一步对“省 token”是间接收益，但会让后续裁剪、stub、summary 全都更稳

  P10 再考虑把 session memory 和 context selector 做更统一的预算仲裁。

  - 当前 session memory 已经能参与 compact，也能按策略注入 prompt
  - 但它和普通 memories / agents / teams 还属于分段决策
  - 后面可以把它们统一进一个“context block scorer + budget allocator”，用 relevance + recency + size 统一裁决
  - 这一步收益更偏长期架构，不是最先做的

  如果只按“减少 token 消耗”排优先级

  1. P8 细粒度 tool result budgeting
  2. P6 getSystemContext() 全量缓存与失效
  3. P7 query-aware teams + team memory
  4. P9 compact boundary + resume-aware projection
  5. P10 统一 context budget scorer

  一句话判断

  如果和 src 对比，mini-v8 现在最大的缺口已经不是“有没有 compact / session memory / projection”，而是：

  - tool result 预算还不够细
  - system context 缓存与失效还不够完整
  - compact boundary / resume 语义还不够强

  如果你要继续，我建议直接开做 P6 + P8，这两项对 token 和稳定性最有价值。


  # P6 和 P8 已经落到 mini-v8。

  P6 这次把 getSystemContext() / getEnhancedContext() 做成了真正的整段缓存，缓存键包含 cwd + config + conversation
  signature + session + revision，并且把失效链独立成了 mini-v8/src/services/context/contextCacheState.ts:1。核心实现都在
  mini-v8/src/context.ts:130。现在 /clear、/compact 相关入口会主动打掉 cache，mini-v8/src/services/messages/
  apiProjection.ts:41；同时 memory/session/agent/team 的变更也会触发 invalidation，所以不会再出现“context 只算一次，然后
  长期吃旧数据”的情况。

  P8 这次在 microcompact 后面又加了一层细粒度 tool-result 预算器：mini-v8/src/services/compact/autoCompact.ts:376。它不
  是按“结果个数”粗暴清理，而是按单条 user tool-result message 的总 token 和单个 result 的体积，稳定地生成 preview/stub；
  只影响 messagesForAPI，不改 fullMessages。投影接线在 mini-v8/src/services/messages/apiProjection.ts:75，/compact 也会
  提示“下一轮使用 budgeted tool result previews”，入口在 mini-v8/src/entrypoints/cli.ts:246。

  验证已经跑过：

  - bun test mini-v8/src/__tests__/context.test.ts mini-v8/src/services/messages/__tests__/apiProjection.test.ts mini-
    v8/src/__tests__/autoCompact.test.ts
  - bun test mini-v8/src/services/memory/__tests__/sessionMemory.test.ts mini-v8/src/services/memory/__tests__/
    memoryStore.test.ts
  - bun run typecheck（在 mini-v8/ 下）

  如果继续，下一步最顺的是 P7 或者把 P8 再往 src/utils/toolResultStorage.ts 那种“resume-aware replacement state”方向补。

  # P7 已经落了。

  现在 teams 不再是全量注入了，改成 query-aware rank + limit，入口在 mini-v8/src/agents/teamManager.ts:300。team memory
  也不再是无差别整段塞 prompt，而是支持 query-aware 选择、limit、字符预算和截断，入口在 mini-v8/src/services/memory/
  teamMemorySync.ts:343。context.ts 里原来空着的 includeTeamMemory 现在已经真正接进 relevance gate 和总预算选择器，mini-
  v8/src/context.ts:351。

  这次还补了一个测试用的 setTeamMemoryDir()，方便隔离 team memory 文件目录，mini-v8/src/services/memory/
  teamMemorySync.ts:74。同时把 team/team-memory 的变化也接进了 context cache invalidation。

  验证已经跑过：

  - bun test mini-v8/src/__tests__/context.test.ts mini-v8/src/services/memory/__tests__/teamMemorySync.test.ts
  - bun run typecheck in mini-v8/

  如果继续，下一步最顺的是 P9：把 compact boundary / resume-aware projection 补起来，让现在这套 fullMessages +
  messagesForAPI + replacement state + query-aware context 在恢复场景下也稳定

  # P9 已经落了，而且现在是和 fullMessages + messagesForAPI + replacement state + query-aware context 配套工作的。

  这次没有把 compact boundary 混进 BetaMessageParam[]，因为 Anthropic 的 BetaMessageParam 只接受 user|assistant。我改成
  了 sidecar metadata 方案：ConversationBuffers 现在额外保存 compactBoundaries，定义在 mini-v8/src/services/messages/
  apiProjection.ts:23 和 mini-v8/src/services/messages/apiProjection.ts:26。projectMessagesForAPI() 会基于最后一个
  boundary 只投影 active slice，mini-v8/src/services/messages/apiProjection.ts:146；一旦发生 forced/full compact，并且启
  用了 commitCompactionToConversation，就会把 compacted active slice 写回 fullMessages，同时追加一条 boundary metadata，
  mini-v8/src/services/messages/apiProjection.ts:176 mini-v8/src/services/messages/apiProjection.ts:241。

  snapshot/restore 也接上了。serializeConversationBuffers() 现在会把 compactBoundaries 一起持久化，mini-v8/src/services/
  messages/apiProjection.ts:94。恢复后，只要把 snapshot 里的 fullMessages + compactBoundaries + toolResultBudgetRecords
  传回 createConversationBuffers()，后续投影就不会再把 pre-boundary 历史重新送回 API。这部分测试在 mini-v8/src/services/
  messages/__tests__/apiProjection.test.ts:247 和 mini-v8/src/services/messages/__tests__/apiProjection.test.ts:275。

  CLI 和 agent 侧也已经切到 commit 模式：

  - mini-v8/src/entrypoints/cli.ts:247
  - mini-v8/src/entrypoints/cli.ts:397
  - mini-v8/src/agents/agentRunner.ts:162

  验证已经跑过：

  - bun test mini-v8/src/services/messages/__tests__/apiProjection.test.ts mini-v8/src/__tests__/autoCompact.test.ts
    mini-v8/src/__tests__/context.test.ts mini-v8/src/services/memory/__tests__/sessionMemory.test.ts mini-v8/src/
    services/memory/__tests__/memoryStore.test.ts mini-v8/src/services/memory/__tests__/teamMemorySync.test.ts
  - bun run typecheck in mini-v8/

  如果继续，下一步最自然的是把这个 snapshot 真正挂到一个本地 session storage/transcript 文件上，这样 P9 就不只是“resume-
  ready projection”，而是“真正可恢复会话”。

  # 把这个 snapshot 真正挂到一个本地 session storage/transcript 文件上

  已经把这一步做成“真正可恢复会话”了，不再只是内存里的 resume-ready projection。

  核心改动在 /D:/claude-code-project/claude-code-best/woyao-code_bcc/mini-v8/src/services/session/sessionStore.ts、/D:/
  claude-code-project/claude-code-best/woyao-code_bcc/mini-v8/src/entrypoints/cli.ts、/D:/claude-code-project/claude-
  code-best/woyao-code_bcc/mini-v8/src/services/memory/sessionMemory.ts。现在 mini-v8 会把会话快照落到本地 ~/.claude-
  code-mini/sessions/<sessionId>.json，内容包含 fullMessages、messagesForAPI 所依赖的 compactBoundaries、
  toolResultBudgetRecords、forceCompactNextProjection，以及 cwd / savedAt / model。CLI 支持 --resume 和
  --resume=<sessionId>，启动时会恢复 snapshot，并尽量恢复原 cwd。每次用户输入、assistant 输出、tool result、/clear、/
  compact 后都会重新保存。

  另外把 session memory 也接回原 session id 了，所以恢复后 full compact 会继续消费之前的 session memory summary，不会变
  成“消息恢复了，但 compact 和记忆断档”。顺手修了一个重要一致性问题：在 committed compact 之后，cli.ts 和 /D:/claude-
  code-project/claude-code-best/woyao-code_bcc/mini-v8/src/agents/agentRunner.ts 不再往旧的 fullMessages 数组引用上追
  加，避免 compact 后写到 stale buffer。

  验证结果：

  - bun run typecheck 通过
  - bun test 全量通过，404 个测试全绿
  - bun run lint 不能跑，因为 mini-v8/package.json 里没有 lint script

  补的测试在 /D:/claude-code-project/claude-code-best/woyao-code_bcc/mini-v8/src/services/session/__tests__/
  sessionStore.test.ts 和 /D:/claude-code-project/claude-code-best/woyao-code_bcc/mini-v8/src/services/memory/__tests__/
  sessionMemory.test.ts，覆盖了磁盘 round-trip、compact boundary 恢复、replacement state 重放、session memory 续接。下一
  步如果你要继续，我建议直接做“snapshot + append-only transcript 双轨”，把现在这个 JSON snapshot 作为 resume fast-path，
  再补一个更像 src/utils/sessionStorage.ts 的可调试 transcript。
